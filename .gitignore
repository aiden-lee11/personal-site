# Node / Next.js
node_modules/
site/.next/
site/out/
site/.turbo/
site/next-env.d.ts

# Compiler ships as prebuilt artifacts under compiler-bin/ (built by the private
# repo's build-binaries workflow — see compiler-bin/README.md). Those binaries
# are tracked; nothing to ignore here.

# OS / editor
.DS_Store
*.log
.vscode/
.idea/

# tmp
scratchpad/
tmp/
